

const Connect = () => {
    // interface Fields {
    //     string
    // }
    const availableFields = [
        "Tech industry", "Agriculture", "adventure", "Animals", "Engineering", "Touring", "Marketing", "Artifitial INtelligence"
    ]

    const selectedFields = []

    availableFields.forEach((fields) => {
        
        if(fields.toLowerCase() === )
    }) 

  return (
    <div> 
        <h1>which Fields are you interested in?</h1>
    </div>
  )
}

export default Connect